import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);


// hook -> it is a utility function .it is used to do a particular work
//custom hook -> can also be created as a utility function

//npm i axios -> it is used to call api
